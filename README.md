# pubsub-chat-application-poc
I made this for a poc, advice from documentation points that a more robust approach should be utilized. but this worked so fine, so fast

server
```apollo-server```

client 
``` apollo-client ```
